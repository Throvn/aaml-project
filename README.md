# {Insert Title Here}: Fine-tuning a language model to predict titles

Code lives under `project/`.

Read the report first.

Unfortunately the fine tuned models and checkpoints are too big to upload. Therefore, the runs directory is on the `.gitignore` list. If you want a live demo, visit: https://throvn.github.io/abstract-title-generator/

However, the models can easily be recreated by executing the python scripts in `project/2_finetuning/*.py`

Note: For the evaluation script, you need python 3.11, otherwise scispacy won't work.
